# Калькулятор расчёта рейкбэка Fish Buffet на ПокерОк

**Fish Buffet Calculator** `fishbuffet.py` — это консольный Python-скрипт, который выполняет расчёты по системе Fish Buffet и помогает игрокам оценивать рейкбэк, прогресс по статусам, эффективный EV от сундуков и потенциальную доходность гринда.

---

## 🐟 Основные возможности

Скрипт `fishbuffet.py` позволяет:

### 📌 1. Анализировать текущий статус Fish Buffet

* Сколько очков требуется до следующего уровня.
* Сколько рейка нужно в сумме.
* Сколько дней потребуется до апгрейда при заданном рейке/сутки.

### 📌 2. Рассчитать эффективный рейкбэк

* Ожидаемая награда за сундук.
* Эффективный `% RB` с учётом рейка, необходимого для уровня.

### 📌 3. Моделировать доходность за период

* Расчёт Fish Points за N дней.
* Расчёт ожидаемого числа сундуков.
* Денежный EV рейкбэка за период.
* Средний RB/день.

### 📌 4. Выполнять Monte Carlo симуляции сундуков

* Случайная генерация наград по вероятностям.
* Расчёт:

  * минимума,
  * среднего,
  * медианы,
  * квартилей,
  * максимума.
* Оценка разброса награды, а не только среднего EV.

### 📌 5. Сравнивать статусы между собой

* На одинаковом объёме рейка и одинаковом периоде.
* Вывод табличного сравнения доходности.

---

## 🧩 Поддерживаемые режимы

* **Интерактивный режим** (при запуске без аргументов)
* **CLI-аргументы** (через `argparse`)
* **Режим сравнения статусов**
* **Режим симуляции наград**

---

## 🚀 Установка и запуск

### Требования

* Python **3.9+**
* Только стандартная библиотека (никаких внешних зависимостей)

### Клон репозитория

```bash
git clone https://github.com/PokerOk-GG/Fish-Buffet-Calculator
cd Fish-Buffet-Calculator
```

---

## ▶️ Примеры запуска

### 1. Интерактивный запуск

```bash
python fishbuffet.py
```

### 2. Расчёт для статуса Shark

```bash
python fishbuffet.py --status shark --points 3500 --daily-rake 50 --period-days 30
```

### 3. Симуляция сундуков

```bash
python fishbuffet.py \
    --status shark \
    --points 3500 \
    --daily-rake 50 \
    --period-days 30 \
    --simulate \
    --iterations 20000
```

### 4. Сравнение всех статусов

```bash
python fishbuffet.py --daily-rake 40 --period-days 30 --compare-statuses
```

---

## 📄 Доступные аргументы

| Аргумент             | Описание                                 |
| -------------------- | ---------------------------------------- |
| `--status, -s`       | Текущий статус Fish Buffet               |
| `--points, -p`       | Текущие Fish Points                      |
| `--daily-rake, -r`   | Средний рейк в день (в $)                |
| `--period-days`      | Число дней для расчёта EV                |
| `--simulate`         | Включить симуляцию сундуков              |
| `--iterations`       | Количество итераций симуляции            |
| `--chests`           | Количество сундуков для ручной симуляции |
| `--compare-statuses` | Сравнить доходность всех статусов        |

---

## 🛠️ Структура проекта

```
Fish-Buffet-Calculator/
│
├── fishbuffet.py       # основной скрипт
├── README.md           # документация
└── LICENSE             # MIT лицензия
```

---

## 📘 Лицензия

Проект распространяется под лицензией **MIT**. Вы можете свободно использовать, изменять и распространять код.
